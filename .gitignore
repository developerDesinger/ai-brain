# Build artefacts (regenerated by `npm run build` / install.sh)
core/dist/
core/node_modules/

# User data — never commit to the engine repo. Project KB lives in
# <each-project>/.ai-brain/ which has its own .gitignore for the index.
knowledge/global/
knowledge/projects/
knowledge/registry.json

# Editor + OS
.DS_Store
*.log
.idea/

# Per-machine notes
.claude/
*.local
config.bat
node_modules
temp_auto_push.bat
temp_interactive_push.bat
